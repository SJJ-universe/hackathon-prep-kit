# 03 호스팅·심사위원 시연 — 3경로 (빠른 순서)

심사 시점에 "실제 작동·시연 가능"이 인정 조건이다(목업·발표자료만은 불인정). 심사위원에게 보여주는 방법을 빠른 순서로 3개 준비한다. **1순위 Netlify Drop**, 막히면 **GitHub Pages**, 그래도 안 되면 **로컬 폴백**. 시연 직전 점검과 QR은 맨 아래.

> Netlify의 현재 화면 기준 단계별 상세(드롭 1시간 클레임·CLI v26·고정 슬러그·SPA 리다이렉트·Functions로 키 숨기기·무료 크레딧 한도·카카오맵 도메인 등록)는 [06 Netlify 배포 상세 가이드](06_Netlify_배포_상세가이드.md)를 본다.

전제: 보여줄 산출물은 **정적 파일 묶음**(빌드형이면 `npm run build`의 `dist`, 정적 HTML이면 그 폴더)이다. 서버사이드 API 프록시가 있으면 그 부분은 호스팅이 별도로 필요하다(아래 각 경로의 "프록시 주의" 참고).

---

## 경로 1 — Netlify Drop (무인증·가장 빠름) [1순위]

브라우저에서 `https://app.netlify.com/drop` 을 열고, **배포할 폴더를 통째로 드래그&드롭**한다(zip은 먼저 압축 해제, `index.html`은 폴더 루트). 수 초 안에 `https://<랜덤이름>.netlify.app` URL이 발급된다. **발표용은 먼저 로그인 후 드롭하라** — 무로그인으로 만든 사이트는 1시간 내 클레임하지 않으면 소멸한다(공식: "claim within one hour").

절차:
1. 산출물 폴더를 확정한다. 빌드형: `npm run build` → `dist`. 정적: `index.html`이 들어있는 폴더.
2. 그 폴더를 `app.netlify.com/drop`에 드래그. (zip으로 올려도 됨)
3. 발급된 https URL을 복사 → 다른 기기에서 즉시 확인.
4. (선택) 파일을 고쳤으면 같은 페이지에 **다시 드래그**하면 새 URL/갱신. CLI를 쓰면 `netlify deploy --dir dist --prod`.

예상 함정:
- **빌드 없는 정적이라 즉시 뜬다**는 게 장점. 단 **상대경로**로 자산을 참조해야 한다(`/assets/...` 절대경로는 루트 배포라 보통 OK지만, 하위경로 가정 코드는 깨질 수 있음).
- **캐시**: 재배포 후 옛 화면이 보이면 강력새로고침(Ctrl+F5) 또는 시크릿창.
- **HTTPS 필요 기능**은 Netlify가 https라 정상 동작(위치정보·카메라·클립보드·서비스워커). 로컬 http와 달리 여기선 막히지 않는다.
- **프록시 주의**: 브라우저에서 외부 공개 API를 직접 부르면 CORS·키 노출 문제가 난다. 키가 필요한 호출은 Netlify Functions(서버리스)로 옮기거나, 시연용으로 **사전 캐시한 응답 JSON**을 정적으로 함께 올린다(화면엔 출처칩 유지). 별도 FastAPI 프록시를 쓰는 구조면 그 프록시는 로컬 또는 Vercel/Render 등에 따로 띄우고 프론트의 API base만 그 주소로.

---

## 경로 2 — GitHub Pages (gh로 repo + pages) [2순위]

정적 사이트를 GitHub에 올리고 Pages로 공개한다. URL은 `https://<아이디>.github.io/<저장소>/` 형태. 반영에 1~2분 걸린다.

평이한 정적(루트에 index.html) 절차:
```powershell
gh auth status                                  # 로그인 확인 (사전 완료)
gh repo create <repo> --public --source=. --push
# Pages 켜기 (main 브랜치 루트)
gh api --method POST repos/<아이디>/<repo>/pages -f "source[branch]=main" -f "source[path]=/"
# 또는 GitHub 웹 Settings > Pages 에서 Source=main /(root) 선택
```
빌드형(vite 등) 절차:
```powershell
# vite.config 에 base 설정 필요 (하위경로 배포라서)
#   export default { base: '/<repo>/' }
npm run build
npx gh-pages -d dist        # dist를 gh-pages 브랜치로 푸시 (gh-pages 패키지)
# Pages Source = gh-pages /(root)
```

예상 함정:
- **base 경로**: 프로젝트 페이지는 `https://id.github.io/repo/` 하위라, 자산을 절대경로(`/assets`)로 박으면 404. vite는 `base: '/repo/'`, 순수 HTML은 상대경로(`./assets/...`) 사용. (사용자 페이지 `id.github.io`는 base가 `/`라 이 문제 없음)
- **반영 지연**: 켠 직후 404는 정상, 1~2분 기다린 뒤 새로고침. 첫 공개는 더 걸릴 수 있음.
- **SPA 라우팅 404**: 클라이언트 라우터를 쓰면 새로고침 시 404 — 해시 라우팅을 쓰거나 `404.html`을 `index.html` 복사본으로 둔다. 단일 화면이면 무관.
- **프록시 주의**: GitHub Pages는 **정적 전용**(서버 없음). 키 필요한 호출은 외부 프록시로 빼거나 사전 캐시 JSON 사용(경로 1과 동일).
- **public repo**: 무료 Pages는 public 권장. 키·`.env`가 커밋되지 않았는지 확인(`.gitignore`).

---

## 경로 3 — 로컬 폴백 (같은 와이파이 또는 화면공유) [최후]

호스팅이 전부 막혀도 시연은 가능하다. 발표 PC에서 로컬 서버를 띄우고, 같은 와이파이의 심사 기기에서 접속하거나 화면공유로 보여준다.

서버 띄우기 — 부트킷의 `serve.ps1`(있으면) 또는 직접:
```powershell
.\serve.ps1                      # 있으면 (포트·디렉터리 기본값으로 기동)
# 없으면 둘 중 하나:
npx http-server ./dist -p 8080   # 정적/빌드 산출물
python -m http.server 8080       # 파이썬만 있을 때
```
같은 와이파이에서 접속:
```powershell
ipconfig                         # IPv4 주소 확인 (예: 192.168.0.12)
# 심사 기기 브라우저에서:  http://192.168.0.12:8080
```

예상 함정:
- **방화벽**: 첫 실행 시 Windows 방화벽 허용 프롬프트가 뜨면 "예"(사설 네트워크 허용). 막히면 접속 안 됨.
- **HTTPS 필요 기능**: 위치정보·카메라 등은 보안 컨텍스트가 필요. **`localhost`는 예외로 http에서도 동작**하지만, `http://<IP>`로 다른 기기에서 열면 막힌다. → 그 기능을 보여줄 땐 **발표 PC 자신의 `http://localhost:8080` + 화면공유/미러링**으로 시연한다.
- **대회장 와이파이 격리**: 게스트망은 기기 간 통신을 막는 경우가 많다. 이때는 같은 와이파이 접속을 포기하고 **화면공유(빔/HDMI/미러링)**로 전환.
- 백업: 정상 플로우 30초 녹화·스크린샷 3장을 PC에 두고, 라이브가 흔들리면 즉시 재생.

---

## 시연 직전 체크 (무대 오르기 전 60초)

- [ ] **다른 기기**(휴대폰/다른 노트북)에서 URL을 직접 열어 봤다
- [ ] 그 기기에서 **새로고침(강력)** 했고 옛 화면 캐시가 아니다
- [ ] **핵심 기능 1장면**(심사위원 즉석입력 → 실호출 → 출처칩)이 그 자리에서 돈다
- [ ] HTTPS 필요 기능을 쓴다면 그 기능이 시연 경로에서 막히지 않는다(경로별 함정 확인)
- [ ] degraded 토글·사람연결 폴백이 뜬다 / 큰글씨·음성·언어전환이 켜진다
- [ ] URL과 QR을 슬라이드/종이에 띄워 심사위원이 **직접 자기 폰으로** 열어볼 수 있다
- [ ] 백업(30초 녹화·스크린샷) 재생 준비됨

발표 동선·역할 분담은 [`../06_pitch/8min-runbook.md`](../06_pitch/8min-runbook.md), 통과 필수 급소는 [`../06_pitch/common-p0-checklist.md`](../06_pitch/common-p0-checklist.md).

---

## QR 코드 만드는 법 (한 줄)

- 오프라인(추천·망 불안 대비): `npx qrcode "https://<배포URL>" -o qr.png` → 슬라이드/종이에 삽입.
- 온라인(빠를 때): qr-code-generator류 사이트에 URL 붙여넣기 → PNG 저장. (현장 망이 불안하면 오프라인 생성이 안전)
